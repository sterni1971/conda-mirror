source: robostack
destination: ./robostack-fs-mirror

# include all except the following
exclude:
  # you can use MatchSpecs here
  - jupyter-ros ==0.5.0
  # or globs over names
  - name-glob: "*"
    matchspec: "[license=AGPL-3.0-or-later]"
